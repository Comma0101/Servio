apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: db-password-provider
spec:
  provider: gcp
  parameters:
    secrets: |
      - resourceName: "projects/serviio-465422/secrets/db-password/versions/latest"
        path: "db_password"
  secretObjects:
    - secretName: "db-password"
      type: "Opaque"
      data:
        - key: "latest"
          objectName: "db_password"
